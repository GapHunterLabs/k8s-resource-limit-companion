<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.k8sresourcelimitcompanion</id>
    <name>K8s Resource Limit Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on any container entry in a Kubernetes workload
        manifest (Deployment, Pod, StatefulSet, DaemonSet, Job, CronJob)
        with no <code>resources:</code> block, or one missing
        <code>requests</code>/<code>limits</code>. Without requests, the
        scheduler can't make good placement decisions; without limits, a
        single container can consume unlimited CPU/memory and starve
        its neighbors on the same node -- a real, common, and
        documented Kubernetes footgun.</p>
        <p><b>100% static text analysis</b> -- an indentation-based line
        scanner, not a real YAML parser, so it works whether the real
        Kubernetes/YAML plugin is installed or not. Only scans files
        whose top-level <code>kind:</code> is a workload kind that
        actually runs containers -- a raw ConfigMap/Service/List
        manifest is skipped entirely.</p>
        <p>Stated honestly: v0.1 handles the common single-document,
        `containers:`/`- name: x` shape -- multi-doc files (`---`
        separators) and YAML anchors/aliases aren't specially resolved.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="K8s Resource Limit Companion"
            displayName="Missing resource requests/limits"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.k8sresourcelimitcompanion.inspection.MissingResourceLimitInspection"/>
        <notificationGroup id="K8s Resource Limit Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
